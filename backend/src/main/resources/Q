1. in ErrorResponse, CLIENT_ERROR_CODE -> " errorResponse.setErrorMessage("Duplicate data was found on request"); " duplicate data / duplicate title
   public static final String CLIENT_ERROR_CODE = "001"; ??

2. Mesajul din TaskTableService " throw new DuplicateDataException("Title already exists"); " de ce nu se executa?
    "logger.error("An unknown error occurred", e);"
    in controller catch Exception e si             errorResponse.setErrorMessage("Duplicate data was found on request");  -> e.value()
    in service exceptia sa aibe mesaj (in clasa exceeptie trebuie constructor cu string mesaj)

            if (projectOptional.isEmpty()) {
                throw new NotFoundException();
            }

3. Unde apar erorile din exceptii in frontend? pop out



4. public ResponseEntity<?> showTasksByProjectId(@RequestParam("taskTableId") Long taskTableId)
    pentru un id existent in trecut, ne returneaza o lista goala. id-ul este auto increment


5. task table title / task title  request, sa fie notnull la creearea unui tabel nou.
Raspuns: trebuie sa stergem @notnull la creare si se valideaza pe update la butonul Done